apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: castor-garage-production

resources:
  - namespace.yaml
  - ../../base

patchesStrategicMerge:
  - configmap-patch.yaml

images:
  - name: ghcr.io/castor-garage/mecanica-pos-soat
    newTag: latest
